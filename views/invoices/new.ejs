<div class="container">
  <%- include('../partials/navbar') %>
  <div class="workspaceContainer">
    <%- include('../partials/sidebar', { active: 'invoices' , projectId: projectId }) %>
    <div class="workspace">
      <div class="formBox">
        <h3 class="title">New invoice</h3>
        <form action="/invoices/<%= projectId %>/create" method="POST" class="form grid">
          <div class="row">
            <% if (label) { %>
              <label class="def" for="name">Name</label>
            <% } %>
            <input type="text" id="name" class="input" name="invoiceName" placeholder="Name">
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="unitPrice">Unit Price</label>
            <% } %>
            <input type="number" id="unitPrice" class="input" name="invoiceunitprice placeholder="Unit Price">
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="quantity">Quantity</label>
            <% } %>
            <input type="number" id="quantity" class="input" name="invoiceunitqantity" placeholder="Quantity">
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="">Type</label>
            <% } %>
            <select id="type" class="input" name="invoiceType" size="1">
              <option value="postponed">Postponed</option>
              <option value="cash">Cash</option>
            </select>
          </div>
          <div class="row spanTwo">
            <% if (label) { %>
              <label class="def" for="details">Details</label>
            <% } %>
            <textarea name="invoiceDetails" class="input" id="details" cols="30" rows="10" placeholder="Details"></textarea>
          </div>
          <div class="actionButtons">
            <button type="submit">Add</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>