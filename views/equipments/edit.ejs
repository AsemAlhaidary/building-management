<div class="container">
  <%- include('../partials/navbar') %>
  <div class="workspaceContainer">
    <%- include('../partials/sidebar', { active: 'equipments', projectId: projectId }) %>
    <div class="workspace">
      <div class="formBox">
        <h3 class="title">تعديل معدة</h3>
        <form action="/equipments/<%= projectId %>/edit/<%= equipment.id %>" method="POST" class="form grid">
          <div class="row">
            <% if (label) { %>
              <label class="def" for="name">Name</label>
            <% } %>
            <input type="text" id="name" class="input" name="equipmentName" value="<%= equipment.equipment_name %>" placeholder="الاسم">
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="date">Date</label>
            <% } %>
            <input type="date" id="date" class="input" name="equipmentDate" value="<%= equipment.equipment_date %>" placeholder="التاريخ">
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="unitPrice">Unit Price</label>
            <% } %>
            <input type="number" id="unitPrice" class="input" name="equipmentUnitPrice" value="<%= equipment.equipment_unit_price %>" placeholder="سعر الوحدة">
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="quantity">equipment</label>
            <% } %>
            <input type="number" id="quantity" class="input" name="equipmentUnitQuantity" value="<%=equipment.equipment_unit_quantity %>" placeholder="المعدة">
          </div>
          <div class="row spanTwo">
            <% if (label) { %>
              <label class="def" for="details">Details</label>
            <% } %>
            <textarea name="equipmentDetails" class="input" id="details" cols="30" rows="10" placeholder="البيان"><%= equipment.equipment_details %></textarea>
          </div>
          <div class="actionButtons">
            <button type="submit">تعديل</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>