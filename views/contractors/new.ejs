<div class="container">
  <%- include('../partials/navbar') %>
  <div class="workspaceContainer">
    <%- include('../partials/sidebar', { active: 'contractors', projectId: projectId }) %>
    <div class="workspace">
      <div class="formBox">
        <h3 class="title">New Contractor</h3>
        <form action="/contractors/<%= projectId %>/create" method="POST" class="form grid">
          <div class="row">
            <% if (label) { %>
              <label class="def" for="contractorName">Name</label>
            <% } %>
            <input type="text" id="contractorName" class="input" name="contractorName" placeholder="Name" required>
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="contractorDetails">Details</label>
            <% } %>
            <input type="text" id="contractorDetails" class="input" name="contractorDetails" placeholder="Details" required>
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="contractorWorkUnit">Unit</label>
            <% } %>
            <select id="contractorWorkUnit" class="input" name="contractorWorkUnit" size="1">
              <% workTypes.forEach(workType => { %>
                <option value="<%= workType %>"><%= workType %></option>
              <% }) %>
            </select>
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="contractorWorkQuantity">Quantity</label>
            <% } %>
            <input type="number" id="contractorWorkQuantity" class="input" name="contractorWorkQuantity" placeholder="Quantity">
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="contractorUnitPrice">Price</label>
            <% } %>
            <input type="number" id="contractorUnitPrice" class="input" name="contractorUnitPrice" placeholder="Price">
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="contractorWorkTotal">Total</label>
            <% } %>
            <input type="number" id="contractorWorkTotal" class="input" name="contractorWorkTotal" placeholder="Total">
          </div>
          <div class="actionButtons">
            <button type="submit">Add</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>