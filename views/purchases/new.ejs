<div class="container">
  <%- include('../partials/navbar') %>
  <div class="workspaceContainer">
    <%- include('../partials/sidebar', { active: 'purchases' , projectId: projectId }) %>
    <div class="workspace">
      <div class="formBox">
        <h3 class="title">New Purchase</h3>
        <form action="/purchases/<%= projectId %>/create" method="POST" class="form grid">
          <div class="row">
            <% if (label) { %>
              <label class="def" for="name">Name</label>
            <% } %>
            <input type="text" id="name" class="input" name="purchaseName" placeholder="Name">
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="unitPrice">Unit Price</label>
            <% } %>
            <input type="number" id="unitPrice" class="input" name="purchaseUnitPrice" placeholder="Unit Price">
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="quantity">Quantity</label>
            <% } %>
            <input type="number" id="quantity" class="input" name="purchaseUnitQuantity" placeholder="Quantity">
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="phoneNum">Type</label>
            <% } %>
            <select id="type" class="input" name="purchaseType" size="1">
              <option value="postponed">Postponed</option>
              <option value="cash">Cash</option>
            </select>
          </div>
          <div class="row spanTwo">
            <% if (label) { %>
              <label class="def" for="details">Details</label>
            <% } %>
            <textarea name="purchaseDetails" class="input" id="details" cols="30" rows="10" placeholder="Details"></textarea>
          </div>
          <div class="actionButtons">
            <button type="submit">Add</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>