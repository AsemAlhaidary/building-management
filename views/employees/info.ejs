<div class="container">
  <%- include('../partials/navbar') %>
  <div class="workspaceContainer">
    <%- include('../partials/sidebar') %>
    <div class="workspace">
      <div class="infoBox">
        <h3 class="title">Info <%= employee.employee_name %></h3>
        <div class="field">
          <h3 class="info">Name: <span><%= employee.employee_name %></span></h3>
        </div>
        <div class="field">
          <h3 class="info">Job: <span><%= employee.employee_job %></span></h3>
        </div>
        <div class="field">
          <h3 class="info">Phone Number: <span><%= employee.employee_phone_num %></span></h3>
        </div>
        <div class="row">
        <div class="actionButtons">
          <form action="/employees/<%= projectId %>/report/<%= employee.id %>" method="post">
            <button type="submit">Report</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>