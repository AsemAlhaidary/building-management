<div class="container">
  <%- include('../partials/navbar') %>
  <div class="workspaceContainer">
    <%- include('../partials/sidebar', { active: 'contractorsdepo' , projectId: projectId }) %>
    <div class="workspace">
      <div class="formBox">
        <h3 class="title">New Contractor Deposit</h3>
        <form action="/contractorsdepo/<%= projectId %>/create" method="POST" class="form grid">
          <div class="row">
            <% if (label) { %>
              <label class="def" for="contractor">Contractor</label>
            <% } %>
            <select id="contractor" class="input" name="contractorId" size="1">
              <% contractors.forEach(contractor => { %>
                <option value="<%= contractor.id %>">
                  <%= contractor.contractor_name %>
                </option>
              <% }) %>
            </select>
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="method">Method</label>
            <% } %>
            <select id="method" class="input" name="depositMethod" size="1">
              <% methods.forEach(method => { %>
                <option value="<%= method %>">
                  <%= method %>
                </option>
              <% }) %>
            </select>
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="depositDate">Date</label>
            <% } %>
            <input type="date" id="depositDate" class="input" name="depositDate" placeholder="Date" required>
          </div>
          <div class="row">
            <% if (label) { %>
              <label class="def" for="depositPrice">Price</label>
            <% } %>
            <input type="number" id="depositPrice" class="input" name="depositPrice" placeholder="Price" min="0" required>
          </div>
          <p class="error">
            <% if (messages.error) { %>
              <%- messages.error %>
            <% } %>
          </p>
          <div class="actionButtons">
            <button type="submit">Add</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>